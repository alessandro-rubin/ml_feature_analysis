"""Streamlit dashboard over a ResultStore (read-only, never recomputes).

Run with::

    streamlit run src/tessa/dashboard/app.py -- --root outputs/runs

Requires the ``dashboard`` extra: ``pip install ml-analysis[dashboard]``.
"""
